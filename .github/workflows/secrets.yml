# Skan sekretow. Zastepuje platny GitHub Secret Protection.
# Definicja: Do-More-Soft/.github/.github/workflows/gitleaks.yml
#
# Plik rozkladany centralnie: HubertPtaszek/dashboard -> rollout-security.mjs
name: Sekrety

on:
  push:
    branches: [main, master]
  pull_request:
  workflow_dispatch:

jobs:
  gitleaks:
    uses: Do-More-Soft/.github/.github/workflows/gitleaks.yml@main
